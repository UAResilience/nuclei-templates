id: roundcube-webmail-twofactor

info:
  name: Roundcube Webmail Detection the 2F
  author: Kay
  severity: info
  description: |
    Detects the presence of Roundcube webmail client  from the HTTP response.
  reference: 
    - https://github.com/alexandregz/twofactor_gauthenticator
  tags: webmail, roundcube

http:
  - method: GET
    path:
      - "{{BaseURL}}"
      - "{{BaseURL}}/mail/"

    matchers-condition: and
    matchers:
      - type: regex
        part: body
        regex:
          - "twofactor_gauthenticator"
